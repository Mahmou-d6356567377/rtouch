import 'package:flutter/material.dart';

class DocsScrean extends StatelessWidget {
  const DocsScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [ 
        Text('docs screan')
      ],),
    );
  }
}